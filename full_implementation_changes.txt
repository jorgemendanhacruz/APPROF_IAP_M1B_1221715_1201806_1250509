CNN -> full run, just change SUBSET_FRACTION = 0.10 → SUBSET_FRACTION = 1.0 in cell 2 and re-run.
Early stopping by default:
  - train_model() now accepts early_stop=True, patience=3 — if no EarlyStopping is in callbacks, it injects one automatically. So every
  training call benefits without manual setup.
  - The overfit cell passes early_stop=False (the only place we want training to keep going to demonstrate divergence).

  Caching removed everywhere (cells 11, 22, 28) — that was the main cause of the silent multi-minute freezes you hit before.

  To switch back to a real run later: change SUBSET_FRACTION = 0.05 to 1.0 in cell 2 and bump epoch counts in cells 15, 16, 20, 22, 25,
  29, 30. Everything else (early stopping, EpochTimer, structure) stays the same.

  